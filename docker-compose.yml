services:
  api:
    build: .
    container_name: flowstate-api
    ports:
      - "${FLOWSTATE_PORT:-3001}:${FLOWSTATE_PORT:-3001}"
    environment:
      FLOWSTATE_PORT: ${FLOWSTATE_PORT:-3001}
      FLOWSTATE_API_KEY: ${FLOWSTATE_API_KEY}
    env_file:
      - .env
    restart: unless-stopped
