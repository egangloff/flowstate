services:
  api:
    build: .
    container_name: flowstate-api
    ports:
      - "${PORT:-3001}:${PORT:-3001}"
    environment:
      PORT: ${PORT:-3001}
    env_file:
      - .env
    restart: unless-stopped
